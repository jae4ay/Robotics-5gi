using UnityEngine;

// Cube를 CylA -> CylB -> CylC -> CylD로 순차적 이동
// 속성 : 물체의 속도, Cylinder 배열, 

public class MovementEX2 : MonoBehaviour
{
    public GameObject[] targets;
    
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
